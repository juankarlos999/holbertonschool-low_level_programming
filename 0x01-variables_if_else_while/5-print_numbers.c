#include <stdio.h>
/**
 * main - Funtion that prints the numbers from 0 to 9
 *
 * Return: (0)
 */
int main(void)
{
	int num = 0;

	while (num < 10)
	{
		printf("%d", num);
		num++;
	}
	putchar('\n');

return (0);
}
