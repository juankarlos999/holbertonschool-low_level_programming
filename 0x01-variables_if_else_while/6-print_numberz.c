#include <stdio.h>
/**
 * main - Funtion that prints the numbers from 0 to 9
 *
 * Return: (0)
 */
int main(void)
{
	int num = 48;

	while (num < 58)
	{
		putchar(num);
		num++;
	}
	putchar('\n');

return (0);
}
